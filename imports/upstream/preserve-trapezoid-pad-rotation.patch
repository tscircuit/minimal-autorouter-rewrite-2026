From 7c4b21e5d6cfb71e5211c16909d2f86d591d508d Mon Sep 17 00:00:00 2001
From: seveibar <seveibar@gmail.com>
Date: Sat, 5 Sep 2026 18:31:49 -0700
Subject: [PATCH] fix: preserve trapezoid pad rotation [skip ci]

---
 .../CollectFootprintsStage/process-pads.ts    |  50 ++++----
 tests/trapezoid-pad-rotation.test.ts          | 115 ++++++++++++++++++
 2 files changed, 143 insertions(+), 22 deletions(-)
 create mode 100644 tests/trapezoid-pad-rotation.test.ts

diff --git a/lib/stages/pcb/CollectFootprintsStage/process-pads.ts b/lib/stages/pcb/CollectFootprintsStage/process-pads.ts
index 4b9d64e..b89c3d5 100644
--- a/lib/stages/pcb/CollectFootprintsStage/process-pads.ts
+++ b/lib/stages/pcb/CollectFootprintsStage/process-pads.ts
@@ -481,7 +481,7 @@ export function createSmdPad({
     return
   }
 
-  // Handle standard shapes (circle, oval, rect, roundrect)
+  // A placed pad's angle already includes the footprint rotation.
   const ccwRotationDegrees = pad.at?.angle
 
   if (shape === "circle") {
@@ -542,7 +542,24 @@ export function createSmdPad({
     } as PcbSmtPadPill
 
     ctx.db.pcb_smtpad.insert(smtpad)
-  } else if (shape === "rect" || shape === "roundrect") {
+  } else {
+    // Rectangle fallbacks need the same rotation handling as ordinary pads.
+    // A zero-delta trapezoid is exactly a rectangle. For a tapered trapezoid,
+    // retain its full copper envelope and report the approximation explicitly.
+    let rectangleSize = size
+    if (shape === "trapezoid") {
+      const deltaX = pad.rectDelta?.x ?? 0
+      const deltaY = pad.rectDelta?.y ?? 0
+      if (deltaX !== 0 || deltaY !== 0) {
+        rectangleSize = {
+          x: size.x + Math.abs(deltaY),
+          y: size.y + Math.abs(deltaX),
+        }
+        ;(ctx.warnings ??= []).push(
+          `Trapezoid pad ${pad.number} on ${componentId} has nonzero rect_delta; using its conservative rotated rectangle envelope`,
+        )
+      }
+    }
     const roundrectRatio = pad.roundrectRatio
     let cornerRadius: number | undefined
     if (shape === "roundrect" && roundrectRatio !== undefined) {
@@ -560,16 +577,19 @@ export function createSmdPad({
         pcb_component_id: componentId,
         x: pos.x,
         y: pos.y,
-        width: size.x,
-        height: size.y,
+        width: rectangleSize.x,
+        height: rectangleSize.y,
         layer: layer,
         pcb_port_id: pcbPortId,
-        port_hints: [pad.number.toString()],
+        port_hints: [pad.number?.toString()],
         shape: "rotated_rect",
         ccw_rotation: normalizedCcwRotation,
         corner_radius: cornerRadius,
       } as PcbSmtPadRotatedRect
       ctx.db.pcb_smtpad.insert(rotatedsmtpad)
+      if (ctx.stats) {
+        ctx.stats.pads = (ctx.stats.pads || 0) + 1
+      }
       return
     }
 
@@ -581,30 +601,16 @@ export function createSmdPad({
       pcb_component_id: componentId,
       x: pos.x,
       y: pos.y,
-      width: shouldSwapDimensions ? size.y : size.x,
-      height: shouldSwapDimensions ? size.x : size.y,
+      width: shouldSwapDimensions ? rectangleSize.y : rectangleSize.x,
+      height: shouldSwapDimensions ? rectangleSize.x : rectangleSize.y,
       layer: layer,
       pcb_port_id: pcbPortId,
-      port_hints: [pad.number.toString()],
+      port_hints: [pad.number?.toString()],
       shape: "rect",
       corner_radius: cornerRadius,
     } as PcbSmtPadRect
 
     ctx.db.pcb_smtpad.insert(smtpad)
-  } else {
-    // Default to rect for unknown shapes
-    ctx.db.pcb_smtpad.insert({
-      type: "pcb_smtpad",
-      pcb_component_id: componentId,
-      x: pos.x,
-      y: pos.y,
-      width: size.x,
-      height: size.y,
-      layer: layer,
-      pcb_port_id: pcbPortId,
-      port_hints: [pad.number?.toString()],
-      shape: "rect",
-    } as PcbSmtPadRect)
   }
 
   if (ctx.stats) {
diff --git a/tests/trapezoid-pad-rotation.test.ts b/tests/trapezoid-pad-rotation.test.ts
new file mode 100644
index 0000000..1369e6a
--- /dev/null
+++ b/tests/trapezoid-pad-rotation.test.ts
@@ -0,0 +1,115 @@
+import { expect, test } from "bun:test"
+import { KicadToCircuitJsonConverter } from "../lib"
+
+function convertPad({
+  angle,
+  footprintAngle = 0,
+  side = "F",
+  delta,
+}: {
+  angle: number
+  footprintAngle?: number
+  side?: "F" | "B"
+  delta?: [number, number]
+}) {
+  const source = `(kicad_pcb (version 20240108) (generator pcbnew)
+    (layers (0 "F.Cu" signal) (31 "B.Cu" signal) (44 "Edge.Cuts" user))
+    (gr_rect (start -30 -30) (end 30 30)
+      (stroke (width 0.05) (type default)) (fill none) (layer "Edge.Cuts"))
+    (footprint "Synthetic:Trapezoid" (layer "${side}.Cu")
+      (at 0 0 ${footprintAngle})
+      (uuid "371b4a38-e91b-4d94-84f2-05087890be82")
+      (property "Reference" "C1" (at 0 0) (layer "${side}.SilkS"))
+      (pad "1" smd trapezoid (at -3.4 0 ${angle}) (size 2.5 5.3)
+        ${delta ? `(rect_delta ${delta[0]} ${delta[1]})` : ""}
+        (layers "${side}.Cu" "${side}.Mask" "${side}.Paste"))))`
+  const converter = new KicadToCircuitJsonConverter()
+  converter.addFile("synthetic.kicad_pcb", source)
+  converter.runUntilFinished()
+  const output = converter.getOutput()
+  const pads = output.filter((element) => element.type === "pcb_smtpad")
+  expect(pads).toHaveLength(1)
+  const pad = pads[0]!
+  if (pad.shape !== "rect" && pad.shape !== "rotated_rect") {
+    throw new Error(`Expected a rectangle envelope, received ${pad.shape}`)
+  }
+  expect(pad.pcb_port_id).toBeDefined()
+  expect(output.filter((element) => element.type === "pcb_port")).toHaveLength(
+    1,
+  )
+  expect(converter.getStats().pads).toBe(1)
+  return { pad, warnings: converter.getWarnings() }
+}
+
+for (const side of ["F", "B"] as const) {
+  for (const angle of [0, 90, 180, 270, -90, 450]) {
+    test(`zero-delta trapezoid at ${angle} degrees on ${side}.Cu preserves its dimensions`, () => {
+      const { pad, warnings } = convertPad({ angle, side })
+      const swapsAxes = Math.abs(angle / 90) % 2 === 1
+      expect(pad).toMatchObject({
+        shape: "rect",
+        width: swapsAxes ? 5.3 : 2.5,
+        height: swapsAxes ? 2.5 : 5.3,
+        layer: side === "F" ? "top" : "bottom",
+        x: -3.4,
+        y: 0,
+      })
+      expect(warnings).toEqual([])
+    })
+  }
+
+  for (const angle of [37, -23]) {
+    test(`zero-delta trapezoid at ${angle} degrees on ${side}.Cu preserves its angle`, () => {
+      const { pad, warnings } = convertPad({ angle, side, delta: [0, 0] })
+      expect(pad).toMatchObject({
+        shape: "rotated_rect",
+        width: 2.5,
+        height: 5.3,
+        ccw_rotation: angle < 0 ? angle + 360 : angle,
+        layer: side === "F" ? "top" : "bottom",
+      })
+      expect(warnings).toEqual([])
+    })
+  }
+
+  test(`a rotated footprint on ${side}.Cu does not rotate its pad angle twice`, () => {
+    const { pad } = convertPad({ angle: 270, footprintAngle: -90, side })
+    expect(pad).toMatchObject({ shape: "rect", width: 5.3, height: 2.5 })
+    expect(pad.x).toBeCloseTo(0, 10)
+    expect(pad.y).toBeCloseTo(3.4, 10)
+  })
+
+  test(`an independent pad angle survives an arbitrary footprint rotation on ${side}.Cu`, () => {
+    const { pad } = convertPad({ angle: 12, footprintAngle: 35, side })
+    expect(pad).toMatchObject({
+      shape: "rotated_rect",
+      width: 2.5,
+      height: 5.3,
+      ccw_rotation: 12,
+    })
+    expect(pad.x).toBeCloseTo(-3.4 * Math.cos((35 * Math.PI) / 180), 10)
+    expect(pad.y).toBeCloseTo(-3.4 * Math.sin((35 * Math.PI) / 180), 10)
+  })
+}
+
+for (const delta of [
+  [0.8, 0],
+  [0, -1.2],
+] as [number, number][]) {
+  for (const angle of [90, 37]) {
+    test(`tapered trapezoid ${delta} at ${angle} degrees keeps a conservative envelope and reports it`, () => {
+      const { pad, warnings } = convertPad({ angle, delta })
+      const width = 2.5 + Math.abs(delta[1])
+      const height = 5.3 + Math.abs(delta[0])
+      expect(pad).toMatchObject({
+        shape: angle === 90 ? "rect" : "rotated_rect",
+        width: angle === 90 ? height : width,
+        height: angle === 90 ? width : height,
+      })
+      if (pad.shape === "rotated_rect") expect(pad.ccw_rotation).toBe(angle)
+      expect(warnings).toHaveLength(1)
+      expect(warnings[0]).toContain("nonzero rect_delta")
+      expect(warnings[0]).toContain("conservative rotated rectangle envelope")
+    })
+  }
+}
-- 
2.50.1 (Apple Git-155)

